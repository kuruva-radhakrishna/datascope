# Activity Log


- 2026-07-08 11:55 IST — Built entire DataScope app (engines, UI, chat, tests 16/16), fixed Mongo SRV-DNS issue via MONGODB_URI_FALLBACK, verified Bifrost gpt-4o chat end-to-end in UI, production build OK. Wrote Dockerfile (nginx+node, build-arg secrets), vercel.json, README. Pending: Vercel login+deploy, container engine install+build+push, code zip.

- 2026-07-08 12:40 IST — Feature: chat answers with computed tests now render as a pinned visual card at top of center pane (GroupMeansChart with ±SD error bars, chi-square RateBars, correlation ScatterPlot with fit line, aggregate bars + stat chips + significance badge + computed-locally/AI-narrated badges). Analysis deepened: correlation results carry sampled points, ANOVA groups carry per-group sd, profiler auto-detects strongest categorical→numeric difference via outlier-trimmed ANOVA scan (finds planted Karnataka effect, p<0.0001). 16/16 tests pass, prod build OK, verified in browser. Demo reseeded in Mongo with new profile. NOT deployed yet (user will say when).

- 2026-07-08 13:05 IST — Docker blocked: user is a STANDARD Windows user (no admin, UAC credential-prompts denied), WSL2 cannot be enabled locally. Registry registry.buildathon.meesho.dev is publicly reachable (34.21.202.86). Prepared no-admin path: clean source bundle deploy-datascope-src.tar.gz (52KB) + deploy/cloudshell-build.sh (build+smoke+push from Google Cloud Shell, team_id kuruva-radhakrishna, tag = UTC timestamp). deploy/ and archives excluded from git/docker/vercel/zip.

- 2026-07-08 13:55 IST — Features: downloadable HTML insights report (/api/datasets/:id/report + button); seeded demo conversation with 5 worked analyses (chi-square, trimmed t-test, trimmed ANOVA, correlation r=0.92 with scatter, trimmed top-5) visible to every visitor, latest auto-pinned on load; chat answers now crisp bullets (LLM prompt + templates); optional 3xIQR outlier trimming in analysis executor (disclosed in output); seed data gained shipping_fee correlated with price. 17/17 tests, prod build OK, verified in browser. Deploy bundle regenerated (55KB).

- 2026-07-08 14:20 IST - Features: (1) df.head()-style preview pinned at the very top of the center pane, with 5/10/20/50 row selector and its own Excel download; (2) hand-written zero-dependency .xlsx writer (backend/lib/xlsx.js, valid OOXML zip, verified via .NET ZipFile) + export.js mapping every result type (t-test/anova/chisq/correlation/aggregate) to summary+data sheets; (3) Excel download button added to the chat analysis card, the Run-a-test result card, and the head table - all verified firing real downloads in browser. New tests: backend/test/export.test.js (8 tests covering zip structural validity, escaping, sheet-name sanitization, numeric coercion, row limits, per-test-type sheet shapes). 24/24 tests pass, prod build OK. Deploy bundle regenerated (154KB).
